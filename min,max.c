#include<stdio.h>
void main()
{
    int min,max,i,n,a[65];
    printf("enter the number");
  
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    a[0]=min=max;
    if(a[i]>max)
{
    printf("\nmax value is:",a[i]);
}
   if(a[i]<max)
   {
       printf("\nmin value is:",a[i]);
   }
   }

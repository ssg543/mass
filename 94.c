#include <stdio.h>
void main()
{
    int N[5],K,i;
    printf("enter the N value:");
    for(i=0;i<=N[i];i++)
    {
        scanf("%d",&N[i]);
    }
    printf("enter the K value:");
    scanf("%d",&K);
      if(N[i]==K)
      {
          printf("number is%d");
      }
    }
